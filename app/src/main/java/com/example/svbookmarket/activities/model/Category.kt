package com.example.svbookmarket.activities.model

import com.example.svbookmarket.activities.common.Constants

//data class Category(var  imageURL: Uri, var name: String)

data class Category(var backgroundResId:Int, var name: Constants.CATEGORY)
